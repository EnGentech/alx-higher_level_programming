def is_same_class(obj, a_class):
    """A program to return an instance of a
    class base on obj and class provided"""

    if type(obj) is a_class:
        return True
    else:
        return False
